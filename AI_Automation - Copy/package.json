{
  "name": "playwright-demo",
  "version": "1.0.0",
  "description": "Playwright Test Framework",
  "scripts": {
    "postinstall": "npx playwright install chromium chrome firefox webkit",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:ui": "npx playwright test --ui",
    "clean": "rimraf test-results/*"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/dotenv": "^6.1.1",
    "@types/node": "^20.19.27",
    "dotenv": "^17.2.2",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  }
}
